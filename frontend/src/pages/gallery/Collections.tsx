import { GalleryPlaceholder } from './Placeholder';

export default function Collections() {
  return (
    <GalleryPlaceholder
      title="Collections"
      description="Choose which Shopify collection becomes your shoppable social gallery. Products, variants, images and inventory will sync automatically — nothing to upload or tag."
      sprint="Sprint 1 (live catalogue)"
    />
  );
}
