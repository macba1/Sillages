import { GalleryPlaceholder } from './Placeholder';

export default function Publish() {
  return (
    <GalleryPlaceholder
      title="Publish"
      description="Publish the gallery to your store with one click, and turn it off again without leaving a trace. Your theme code is never edited."
      sprint="Sprint 2 (Theme App Extension)"
    />
  );
}
